# Swift / SwiftPM build
.build/
.swiftpm/

# macOS and build artifacts (.app/.dmg files are distributed through GitHub Releases)
.DS_Store
SkillManager.app/
*.dmg

# Local tool metadata (not part of the project)
.claude/
.remember/
